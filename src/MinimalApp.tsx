// MinimalApp.tsx - Ultra minimal React component for debugging
import React from 'react';

const MinimalApp = () => {
  return (
    <div style={{
      padding: '20px',
      fontFamily: 'Arial, sans-serif',
      textAlign: 'center'
    }}>
      <h1>Minimal App</h1>
      <p>This is a minimal React component with zero dependencies.</p>
    </div>
  );
};

export default MinimalApp;
